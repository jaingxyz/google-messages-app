# This project needs NO secrets — your session lives in the paired Chromium profile.
#
# Optional: override where the persistent browser profile is stored.
# Defaults to ~/Library/Application Support/google-messages-mcp/profile
# GM_PROFILE_DIR=/absolute/path/to/profile
